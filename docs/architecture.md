# 架构草图

## 1. 顶层模块

### Core

负责以下内容：

- 媒体信息模型
- 任务定义
- 预设定义
- 外部工具抽象
- 命令生成
- 执行与日志模型

### Cli

负责以下内容：

- 调试入口
- 批处理脚本入口
- 命令预览
- 回归验证辅助

### Desktop

负责以下内容：

- 媒体文件导入
- 任务队列视图
- 预设编辑器
- 执行进度与日志界面

## 2. 核心数据流

```text
输入文件
  -> 媒体探测
  -> 生成规范化媒体信息
  -> 应用预设
  -> 生成任务定义
  -> 构建外部命令
  -> 执行任务
  -> 采集日志与产物元数据
```

## 3. 关键对象建议

- `MediaProbeResult`
- `PresetDefinition`
- `JobDefinition`
- `CommandPlan`
- `ExecutionResult`

## 4. 后续演进方向

- 抽象多编码器支持
- 引入预设迁移器
- 引入任务历史与恢复
- 建立兼容旧工具预设的导入器

